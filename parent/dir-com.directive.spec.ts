import { DirComDirective } from './dir-com.directive';

describe('DirComDirective', () => {
  it('should create an instance', () => {
    const directive = new DirComDirective();
    expect(directive).toBeTruthy();
  });
});
